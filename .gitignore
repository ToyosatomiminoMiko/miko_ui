prompt.md
# dependencies
/node_modules

# build output
/dist

# TypeScript 增量缓存
*.tsbuildinfo

# release 资产(`npm run release:pack` 的产出:stage/ + miko_ui_dist.tar.gz)
# 它不是交付形态本身 —— 交付形态是 GitHub 上 `ui-latest` 那个 release 资产,
# 见 RELEASING.md.这里只是本地/CI 打包时的落地目录,不入库.
/release

# npm 凭据(通过 NPM_CONFIG_USERCONFIG 指到本目录时用;里面是明文 token,绝不能提交)
/.npmrc

# npm 打包产物
/*.tgz

