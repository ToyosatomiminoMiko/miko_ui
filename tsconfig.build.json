{
  /**
   * 构建 `dist/` 用的配置:在 `tsconfig.json` 之上只改"要不要产出文件"这件事.
   *
   * 产物**不是给 npm 发布的**(库不发 npm,见 RELEASING.md),而是给唯一消费者
   * `miko_graphcalc` 用的:`fetch_ui.sh` 在本地 clone 之后构建它,应用通过
   * `"@miko/ui": "file:packages/miko_ui"` + `exports` 解析到 `dist/index.js` /
   * `dist/index.d.ts`.所以"产物干不干净"的判据是**那个消费者认不认**,不是
   * "发布出去别人能不能用".
   *
   * 为什么不复用同一份配置:开发配置里 `noEmit: true`(类型检查专用,不落盘),
   * 而且 `include` 把 `test/**` `example/**` 都收进来了.产物里既不该有测试,
   * 也不该有 example,所以这里必须换一份 `include`/`exclude`.
   *
   * 为什么不把 `noEmit` 反过来写进 `tsconfig.json`:那样 `npm run typecheck`
   * 会顺手往工作区里丢一堆 JS;两者目标不同,分开两份最省解释.
   */
  "extends": "./tsconfig.json",
  "compilerOptions": {
    // 参考配置是 noEmit,这里显式打开产出.
    "noEmit": false,
    "declaration": true,
    // 产物落在包根的 dist/;`rootDir: src` 保证目录结构 1:1,不会多出 `src/` 这一层.
    "outDir": "dist",
    "rootDir": "src",
    /**
     * 打进产物的库代码不碰 Node(测试才用 `node:fs`),所以这里把 `types` 清空:
     * 万一将来有人在库里误引 `node:*`,这一步会直接编译失败,而不是悄悄把
     * "这个库需要 Node 环境"带进产物.
     *
     * 注意这里**不**再补 ESM 说明符:库内写的是无扩展名相对导入,`tsc` 原样输出,
     * 由消费者的打包器解析 -- 唯一消费者用 Vite,它自带解析器.这一点以前由一个
     * 构建后处理脚本(`fix_esm_specifiers.mjs`)负责,那是为"发 npm 给 Node 原生
     * ESM / `node16` 解析用"准备的,npm 那条路作废之后它就没有对象了.
     */
    "types": []
  },
  "include": ["src/**/*.ts"],
  "exclude": ["node_modules", "dist", "src/**/*.test.ts"]
}
